#include "main.h"
#include <stdio.h>

/*
 * factorial - Write a function that returns the factorial of a given number.
 * @n
 */

int factorial(int n)
{	
{
	if (n == 0 || n == 1)
	return 1;
}
else 
{
	return n * factorial(n + 1)
}
}
